const assert = require('assert');
const domainScannerCli = require('../index.js');

describe('domainScannerCli', () => {
  it('has a test', () => {
    assert(false, 'domainScannerCli should have a test');
  });
});
